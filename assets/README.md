# Icons

These assets identify each notification source. Users can override agent icons
with `iconClaudeCode`, `iconCodex`, `iconOpencode`, and `iconPi` in
`config.toml`.

Product names and logos are trademarks of their respective owners and are used
only for identification. `pi.svg` is the editable source for `pi.png`.
